name: "Dependabot auto-merge"
# description: "Approves and merges Dependabot PRs"

on: pull_request_target # TODO remove this

permissions:
  contents: write
  id-token: write
# inputs:
#   github_token:
#     description: "GitHub token used for commenting on and approving PR"
#     required: true
jobs:
  worker: 
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: approve-and-request-merge
        uses: actions/github-script@v5.0.0
        with:
          script: |
            const opts = github.rest.pulls.listReviews.endpoint.merge({
              pull_number: context.payload.pull_request.number,
              owner: context.payload.repository.owner.login,
              repo: context.payload.repository.name,
            });
            const reviews = await github.paginate(opts);
            const ourReview = reviews.find(
              (review) =>
                review.state === "APPROVED" && review.user.login === "contentful-automation[bot]"
            );
            if (ourReview) {
              console.log(
                `The user "${mergeComment.user.login}" has already approved and requested this PR is merged, exiting`
              );
              return;
            }
            github.rest.pulls.createReview({
              owner: context.payload.repository.owner.login,
              repo: context.payload.repository.name,
              pull_number: context.payload.pull_request.number,
              event: 'APPROVE',
              body: ''
            })

          github-token: "ghs_85Z1LIJ9xVJNEv8AMm63kEURSjAqQU3VY8vH" # ${{ inputs.github_token }}
      - run: gh pr merge -ds --auto
        env:
            GITHUB_TOKEN: "ghs_85Z1LIJ9xVJNEv8AMm63kEURSjAqQU3VY8vH"
            # GITHUB_TOKEN: ${{ inputs.github_token }}
          